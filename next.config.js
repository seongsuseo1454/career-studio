// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',           // 이 줄 통째로 삭제하거나 주석 처리
  // trailingSlash: true,        // 이 것도 같이 삭제
  // images: { unoptimized: true }  // 이 것도 같이 삭제

  // ← 이렇게 빈 상태로 두거나, 다른 설정 있으면 그대로 두기
};

module.exports = nextConfig;